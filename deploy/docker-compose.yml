# AnkaLoop — Docker Compose example
#
# Start:
#   docker compose up -d
#
# Logs:
#   docker compose logs -f
#
# With config:
#   Place your config.toml in the same directory and it will be mounted.

services:
  ankaloop:
    build: ..
    ports:
      - "8080:8080"
    volumes:
      - ./config.toml:/root/.config/amcp/config.toml:ro
      - ankaloop-data:/root/.config/amcp
    environment:
      - PYTHONUNBUFFERED=1
      # - ANKA_MODEL_API_KEY=sk-xxxx
    restart: unless-stopped

volumes:
  ankaloop-data:
